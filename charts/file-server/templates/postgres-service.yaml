apiVersion: v1
kind: Service
metadata:
  name: postgres-{{ .Values.environment }}-service
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    {{- include "postgres.labels" . | nindent 4 }}
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: 5432
      nodePort: {{ .Values.postgres.nodePort }}
