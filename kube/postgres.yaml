---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: misc
spec:
  type: NodePort
  selector:
    app: file-server
  ports:
    - port: 5435
      targetPort: 5432
      nodePort: 35435
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pg-pv
  namespace: misc
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /home/macnod/common-lisp/file-server/pg-data-dev
    type: Directory
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-pvc
  namespace: misc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  volumeName: pg-pv
  storageClassName: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
  namespace: misc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: postgres
          image: postgres:16.10
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - c
                  - pg_ctl stop -D /var/lib/postgresql/data -m fast -w -t 45
          imagePullPolicy: Never
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "fileserver"
            - name: POSTGRES_USER
              value: "fileserver"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: file-server-dev-secrets
                  key: postgres-password
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: pg-data-volume
            - mountPath: /docker-entrypoint-initdb.d
              name: init-sql-volume
      volumes:
        - name: pg-data-volume
          persistentVolumeClaim:
            claimName: pg-pvc
        - name: init-sql-volume
          hostPath:
            path: /home/macnod/common-lisp/file-server/db-init
            type: Directory
